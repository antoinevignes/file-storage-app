/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "knowing-spaniel-571.convex.cloud",
      },
    ],
  },
};

export default nextConfig;
